import React from 'react';
import banner from './banner.svg';

const Header = () => {

    return (
        <header>
            <img src={banner} alt=""/>                       
        </header>
    );
};

export default Header;